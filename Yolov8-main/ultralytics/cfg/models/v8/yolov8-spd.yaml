# Ultralytics YOLO 🚀, AGPL-3.0 license
# YOLOv8-SPD for VisDrone (Small Object Detection)

# Parameters
nc: 10  # classes (根据你的 VisDrone 数据集修改，通常是 10)
scales: # model compound scaling constants, i.e. 'n', 's', 'm', 'l', 'x'
  n: [0.33, 0.25, 1024]
  s: [0.33, 0.50, 1024]
  m: [0.67, 0.75, 768]
  l: [1.00, 1.00, 512]
  x: [1.00, 1.25, 512]

# YOLOv8.0n backbone
backbone:
  # [from, repeats, module, args]
  - [-1, 1, Conv, [64, 3, 2]]  # 0-P1/2  (第一层保持普通卷积，因为输入图很大，SPD计算量会太大)
  - [-1, 1, SPDConv, [128, 3]] # 1-P2/4  <--- 替换 (原本是 Conv stride=2)
  - [-1, 3, C2f, [128, True]]  # 2
  - [-1, 1, SPDConv, [256, 3]] # 3-P3/8  <--- 替换
  - [-1, 6, C2f, [256, True]]  # 4
  - [-1, 1, SPDConv, [512, 3]] # 5-P4/16 <--- 替换
  - [-1, 6, C2f, [512, True]]  # 6
  - [-1, 1, SPDConv, [1024, 3]] # 7-P5/32 <--- 替换
  - [-1, 3, C2f, [1024, True]]  # 8
  - [-1, 1, SPPF, [1024, 5]]   # 9

# YOLOv8.0n head
head:
  - [-1, 1, nn.Upsample, [None, 2, 'nearest']]
  - [[-1, 6], 1, Concat, [1]]  # cat backbone P4
  - [-1, 3, C2f, [512]]        # 12

  - [-1, 1, nn.Upsample, [None, 2, 'nearest']]
  - [[-1, 4], 1, Concat, [1]]  # cat backbone P3
  - [-1, 3, C2f, [256]]        # 15 (P3/8-small)

  - [-1, 1, Conv, [256, 3, 2]]
  - [[-1, 12], 1, Concat, [1]] # cat head P4
  - [-1, 3, C2f, [512]]        # 18 (P4/16-medium)

  - [-1, 1, Conv, [512, 3, 2]]
  - [[-1, 9], 1, Concat, [1]]  # cat head P5
  - [-1, 3, C2f, [1024]]       # 21 (P5/32-large)

  - [[15, 18, 21], 1, Detect, [nc]]  # Detect(P3, P4, P5)