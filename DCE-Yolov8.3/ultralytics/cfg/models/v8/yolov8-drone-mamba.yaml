# YOLOv8-Mamba-Drone V2 - Enhanced Configuration
nc: 10
scales: 
  n: [0.33, 1.00, 1024]

backbone:
  # --- P1/2 ---
  - [-1, 1, Conv, [64, 3, 2]]       # 0
  - [-1, 1, DCE, [64]]              # 1
  
  # --- P2/4 (关键小目标层) ---
  - [-1, 1, Conv, [128, 3, 2]]      # 2
  - [-1, 1, DCE, [128]]             # 3
  - [-1, 2, ERB, [128, 2]]          # 4 → Head 拼接点
  
  # --- P3/8 ---
  - [-1, 1, SCDown, [256, 3, 2]]    # 5
  - [-1, 1, C2f_VSS, [256, 1]]      # 6 ★ 新增:  P3 也用 Mamba
  - [-1, 1, ERB, [256, 2]]          # 7 → Head 拼接点
  
  # --- P4/16 ---
  - [-1, 1, SCDown, [512, 3, 2]]    # 8
  - [-1, 1, C2f_VSS, [512, 2]]      # 9 ★ 增加 n=2
  - [-1, 1, SPPF, [512, 5]]         # 10

head:
  # --- Top-down:  P4 -> P3 ---
  - [-1, 1, nn.Upsample, [None, 2, 'nearest']]  # 11
  - [[-1, 7], 1, Concat, [1]]                    # 12
  - [-1, 1, LSKBlock, [256]]                     # 13 ★ 新增: LSK 注意力

  # --- Top-down: P3 -> P2 ---
  - [-1, 1, nn.Upsample, [None, 2, 'nearest']]  # 14
  - [[-1, 4], 1, Concat, [1]]                    # 15
  - [-1, 1, ERB, [128, 2]]                       # 16 → Detect (Tiny)

  # --- Bottom-up: P2 -> P3 ---
  - [-1, 1, SCDown, [256, 3, 2]]                # 17
  - [[-1, 13], 1, Concat, [1]]                  # 18
  - [-1, 1, ERB, [256, 2]]                      # 19 → Detect (Small)

  # --- Bottom-up: P3 -> P4 ---
  - [-1, 1, SCDown, [512, 3, 2]]                # 20
  - [[-1, 10], 1, Concat, [1]]                  # 21
  - [-1, 1, C2f_VSS, [512, 1]]                  # 22 → Detect (Medium)

  # Detect Head
  - [[16, 19, 22], 1, Detect, [nc]]